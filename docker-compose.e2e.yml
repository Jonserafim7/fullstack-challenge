# Test-only override: CRASH_SCENARIO scripts a reproducible Crash Point sequence (hundredths) so the
# E2E is deterministic; the base stack stays provably-fair.
services:
  games:
    environment:
      CRASH_SCENARIO: "300,500,200,400"
      BETTING_DURATION_MS: "6000"
