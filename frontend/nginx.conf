server {
    listen 3000;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: unknown paths return index.html (the prerendered shell) so
    # the client-side router can take over (/login, /auth/callback, /auth/silent).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
