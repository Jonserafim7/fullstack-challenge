# CLAUDE.md

The agent guide for this repository lives in **[`AGENTS.md`](./AGENTS.md)** — it is the single source of truth (shared with Codex and other agents). Read it.

This pointer exists so Claude Code, which looks for `CLAUDE.md`, is directed to the canonical file. Make edits in `AGENTS.md`, not here.
